<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/acquiesce"
    android:orientation="vertical"
    tools:context=".ui.activity.ExpressActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/express_tb"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@color/purple_500"
        app:navigationIcon="@drawable/ic_back">
<!--        ST0000001 是唯一能查询的单号因此默认查询这个-->
        <androidx.appcompat.widget.SearchView
            android:id="@+id/express_search"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginVertical="5dp"
            android:background="@drawable/search_bg"
            app:defaultQueryHint="ST0000001"
            android:queryHint="ST0000001"/>
    </androidx.appcompat.widget.Toolbar>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">
            <!--            轮播图 -->
            <com.youth.banner.Banner
                android:id="@+id/express_banner"
                android:layout_width="match_parent"
                android:layout_height="200dp" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/express_recommend"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:background="@color/white"
                android:paddingHorizontal="5dp"
                android:paddingTop="5dp"
                android:paddingBottom="10dp" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/express_list"
                android:layout_width="match_parent"
                android:layout_height="660dp"
                android:layout_marginTop="5dp"
                android:background="@color/white"
                android:paddingHorizontal="16dp"
                android:paddingTop="5dp"
                android:paddingBottom="10dp" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>